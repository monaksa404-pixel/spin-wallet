VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Production site for Supabase signup verification / magic-link redirects (no trailing slash).
# Example: https://spin-wallet-six.vercel.app
VITE_SITE_URL=

# If signup returns "email rate limit exceeded" (429): Supabase built-in mail allows very few confirmation emails per hour.
# Fix: Supabase Dashboard → Authentication → Emails → SMTP Settings → Custom SMTP (Resend, SendGrid, etc.).
